"""
Route modules, one per resource, added on later branches (e.g. a future
courses.py, players.py -- each an APIRouter included under API_V1_PREFIX
in main.py). Empty for now: this is scaffolding only, no business-logic
endpoints yet.
"""
