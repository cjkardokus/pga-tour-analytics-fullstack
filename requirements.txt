# Runtime dependencies for the PySpark pipeline (src/) and the FastAPI
# backend (api/) -- what a fresh `pip install -r requirements.txt` needs to
# actually run `python src/pipeline.py` and `uvicorn api.main:app`, nothing
# more. Test-only deps live in requirements-test.txt instead (see that
# file's header). Resolved from a clean virtualenv seeded with just this
# project's seven direct dependencies (pyspark, fastapi, pydantic,
# SQLAlchemy, psycopg2-binary, uvicorn, python-dotenv, kaggle) at the
# versions below, then `pip freeze`'d -- everything else here is one of
# their transitive dependencies, pinned for reproducibility the same way
# the rest of this list already is.
#
# jupytext/nbformat/jupyter_core/markdown-it-py/mdit-py-plugins/mdurl and
# kagglesdk are NOT notebook tooling we use -- they're transitive
# dependencies the `kaggle` CLI package itself pulls in (it supports
# pushing/pulling Jupyter kernels), so they come along for the ride
# whenever `kaggle` is installed. See README.md's "Getting the data"
# section for what `kaggle` itself is here for.
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
attrs==26.1.0
bleach==6.4.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
fastapi==0.141.1
fastjsonschema==2.22.2
greenlet==3.5.5
h11==0.16.0
idna==3.19
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
jupyter_core==5.9.1
jupytext==1.19.5
kaggle==2.2.4
kagglesdk==0.1.37
markdown-it-py==4.2.0
mdit-py-plugins==0.6.1
mdurl==0.1.2
nbformat==5.11.1
packaging==26.3
platformdirs==4.11.5
protobuf==7.36.0
psycopg2-binary==2.9.12
py4j==0.10.9.9
pydantic==2.13.5
pydantic_core==2.46.5
pyspark==4.2.0
python-dateutil==2.9.0.post0
python-dotenv==1.2.3
python-slugify==8.0.4
PyYAML==6.0.3
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
six==1.17.0
SQLAlchemy==2.0.52
starlette==1.6.0
text-unidecode==1.3
tqdm==4.70.0
traitlets==5.16.1
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.52.4
webencodings==0.6.1
