import Typography from "@mui/material/Typography";

/** Placeholder -- real content (CategoryEnum-driven season/all-time
 * tables, backed by GET /api/v1/leaderboards/*) lands on the
 * "Leaderboards" branch of this five-branch front-end build. */
export default function Leaderboards() {
  return <Typography variant="h4">Leaderboards</Typography>;
}
