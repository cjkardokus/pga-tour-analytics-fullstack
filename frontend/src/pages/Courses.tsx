import Typography from "@mui/material/Typography";

/** Placeholder -- real content (course difficulty rankings, backed by
 * GET /api/v1/courses/*) lands on the "Courses" branch of this
 * five-branch front-end build. */
export default function Courses() {
  return <Typography variant="h4">Courses</Typography>;
}
