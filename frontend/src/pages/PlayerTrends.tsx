import Typography from "@mui/material/Typography";

/** Placeholder -- real content (per-player season history/trend charts,
 * backed by GET /api/v1/players/*) lands on the "Player Trends" branch of
 * this five-branch front-end build. */
export default function PlayerTrends() {
  return <Typography variant="h4">Player Trends</Typography>;
}
