[pytest]
# Puts the project root on sys.path so `import api` and `from tests.fixtures
# import ...` resolve regardless of the working directory pytest is invoked
# from -- api/ has no setup.py/pyproject.toml install step, so this is
# otherwise unresolvable the way a normal installed package would be.
pythonpath = .
testpaths = tests

